/**
 * 
 * @param {number} minutes - converts minutes to seconds, negative numbers work as well.
 * @returns  - the number of minutes.
 */
export default function convertMinutesToSeconds(minutes){
    return minutes*60
}
